---

const { tire } = Astro.props
---
<div class="bg-white shadow-lg rounded-lg overflow-hidden transition-transform transform hover:scale-105 duration-300">
  <a 
    href={tire.image} 
    target="_blank" 
    data-lightbox="image-1" 
    data-title="Descripción opcional" 
    class="block relative group"
  >
    <!-- Imagen del producto con efectos visuales -->
    <img 
      src={tire.image} 
      alt={tire.name} 
      class="w-full h-100 object-cover rounded-lg transition-transform duration-300 group-hover:scale-110"
    />
    <!-- Capa decorativa -->
    <div class="absolute inset-0 bg-gradient-to-t from-black via-transparent to-transparent opacity-0 group-hover:opacity-75 transition-opacity duration-300"></div>
  </a>
</div>
